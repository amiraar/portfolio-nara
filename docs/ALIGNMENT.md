# Alignment against docs/portfolio-strategy-2026.md

This tracks where the repo diverges from the strategy doc and what's being done about it. Update this file whenever an item's status changes — don't let it drift.

## Decision: keep the CMS/DB architecture, don't rebuild on MDX

The strategy doc's default recommendation is MDX-in-repo, no database, no admin panel. This repo already has a working Prisma + Neon Postgres CMS, NextAuth login, and a Pusher/Gemini-powered chat assistant (Kaia) — the exact shape the doc calls "the classic time-sink trap."

Ripping it out now would repeat the mistake the doc warns against: spending more weeks on infrastructure instead of job-search leverage. Instead, per the doc's own carve-out (§3, point 3 — "if you want the portfolio itself to be the showcase, host it as one of your featured projects with its own case study"), this app is being reframed as a flagship project with its own case study rather than torn down.

## Status

| Item | Status | Notes |
|---|---|---|
| Remove unused `openai` dependency | ✅ Done | Never imported; Kaia only uses Gemini (`lib/gemini.js`). |
| Label proprietary projects instead of blank links | ✅ Done | `components/portfolio/Projects.jsx` shows a "Proprietary" badge when `project.link` is empty, instead of silently omitting the link icon. |
| Replace bit.ly links with direct Figma URLs (4 UX projects) | ✅ Done | Real URLs supplied by the repo owner and confirmed public. Wired into `lib/portfolioDefaults.js`. |
| Case-study text for Racker, QuickCue, CPTool | ✅ Done in code | Problem/approach/result writeups from the repo owner, wired into `lib/portfolioDefaults.js` (`caseStudy` field) and rendered via a click-to-expand modal in `components/portfolio/Projects.jsx`. Rewritten once for plain, non-technical language after the owner flagged the first pass as too jargon-heavy for a non-engineer reader (state machines, RBAC internals, etc.) — engineering terms in the `tags` array are left as-is since those are meant as scannable keywords, not prose. |
| Plain-language pass on Hero/About/Experience | ✅ Done | Owner asked for the same non-technical, non-hyperbole treatment outside Projects. Simplified the hero tagline and About paragraphs (cut clichés like "I don't just write backend code — I think in user flows" and the absolute claim "shows up in every PR I write"; softened jargon like "AWS S3" → "cloud file storage" in prose). Simplified the 2 most jargon-heavy Experience entries (SoftwareSeni roles — OAuth/DataTables/soft-delete internals). Left the RSU Mitra Paramedika, Lazismu, and Ahmad Dahlan University entries mostly as-is — already plain, factual resume language with no jargon or embellishment to cut. Tags arrays untouched everywhere (scannable keywords, not prose). |
| Screenshots for Racker, QuickCue, CPTool | ✅ Done in code | All 10 owner-supplied screenshots wired into `public/projects/` and each project's `screenshots` array. 2 were cropped/redacted before publishing: `racker-request-detail.png` (3rd-party participant names blanked out, owner's own name kept) and `cptool-candidate-detail.png` (client name "Riot Games" and staff name blanked out). |
| Confidential business data in screenshots | ✅ Done | Owner flagged these are SoftwareSeni's product, not personal — redacted real financial/scale figures that aren't the owner's to publish: Racker's Rp budget amounts (dashboard table, team-lead history, request detail) and employee headcount ("241"), CPTool's dashboard stats ("153 Total Staff", "79 Total Staff Roles", "443 Total Clients"). UI structure, labels, and workflow all stay visible — only real numbers are blanked. |
| Mobile nav dropdown menu unreadable | ✅ Fixed | Unrelated bug found while auditing mobile responsiveness site-wide (owner asked to check beyond just the Projects section). The dropdown panel in `components/portfolio/Nav.jsx` used `bg-background/98` — Tailwind's default opacity scale doesn't include `98` as a step, so the class silently generated no CSS rule at all, leaving the panel fully transparent and hero content bleeding through behind the menu items. Switched to `bg-background/95` (a valid default step). Verified: About, Experience, Skills, Education, footer/contact, and the Kaia chat widget all render correctly on a 390px viewport — only the nav dropdown was broken. |
| Case-study modal interface | ✅ Fixed | Was broken: screenshots rendered at native size before the case-study text, and the close button scrolled out of view. Now: fixed header (title + close button, always reachable), internally-scrollable body capped at `max-h-[85vh]`, text (Problem/Approach/Result) reads before screenshots, screenshots shown as a compact 2-column thumbnail grid (click to open full-size in a new tab) instead of stacked full-resolution images. Verified on desktop (1440px) and mobile (390px) viewports. |
| Case study + public repo link for Nara itself as flagship project | ❌ Won't do | Owner decision — declined. |
| Add qae-autonomist and lapakpro-app as projects | ✅ Done in code | Owner shared 4 more candidate projects; screened each against the strategy doc's "quality over quantity" and "no tutorial clones" guidance. **Included:** qae-autonomist (public repo, custom Playwright bug-scanning tool with 9 documented findings — differentiated, not covered by any existing project) and lapakpro-app (real client work, modern production stack — Docker, CI, quality gates). **Declined:** sdet-developer (same Playwright/POM skill as qae-autonomist but weaker — public demo site, no custom tooling — would dilute rather than add); training-nodejs (owner's own framing: a close reproduction of a paid course, matches the doc's explicit "tutorial clones pinned as portfolio pieces" anti-pattern). qae-autonomist has both a public repo link and a case study — first project with both, so the card footer was fixed to show a "Case study →" hint alongside the external-link icon instead of only one or the other. lapakpro-app is proprietary client work (owner isn't the sole owner), so it's case-study-only like Racker/QuickCue/CPTool, no link. Both case studies are already written in the plain, non-technical style established above. |
| `usePortfolioContent.test.js` cache tests failing | 🔲 Flagged, not fixed | Pre-existing bug in the caching logic, unrelated to the alignment work — out of scope here. |
| **Live site not reflecting any of the above** | 🔲 Owner action pending | Production content is served from the Postgres DB via `/api/portfolio`, not from `lib/portfolioDefaults.js` — that file is only a fallback used when a DB row is missing, and a `projects` row already exists (old descriptions, old bit.ly links, no `caseStudy`/`screenshots` fields). Code changes alone never reach the live site. Fixed in code: added `caseStudy` and `screenshots` fields to `components/dashboard/editors/ProjectsEditor.jsx`, plus a "Load code defaults" button in `ContentEditor.jsx`. **Owner action required:** log into `/dashboard` → Content → Projects → "Load code defaults" → review → Save, once merged and deployed. |

## Scope discipline

No new CMS/dashboard/chat features until the items above are closed. Further engineering time goes to case-study content and the two blocked items, not new capability — that's the actual failure mode this doc identifies.
